import React from 'react';
import footerStyles from '@/assets/Footer.module.css';

export default function Footer() {
  return (
    <div className={footerStyles.footerContainer}>
      <div className={footerStyles.mainFooter}></div>
    </div>
  );
}
